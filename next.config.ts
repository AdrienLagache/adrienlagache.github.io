/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // basePath: '/adrienlagache.github.io',
  // assetPrefix: '/adrienlagache.github.io/',
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig
